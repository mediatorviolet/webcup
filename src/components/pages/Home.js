import React from "react";
import Layout from "../common/Layout/Layout";

const Home = () => {
    return (
        <Layout>
            <h1>Accueil</h1>
        </Layout>
    );
};

export default Home;
